-- Create the loan-tracking database for the device lending programme.
-- I picked a square-bracketed name so the lab tag survives — in production
-- I would name this `LoanDB` without the prefix.

CREATE DATABASE [oakomolafe6433-LoanDB];
GO
